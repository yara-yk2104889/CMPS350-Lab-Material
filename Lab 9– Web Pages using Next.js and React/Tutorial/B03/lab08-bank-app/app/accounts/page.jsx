import React from 'react'

export default function Accounts() {
    return (
        <>
            <h1>Welcome to /accounts</h1>
        </>
    )
}

/*
    JSX Rules
    - Always return a single parent element
    - If an HTML element attribute is a reserved word in JS, use camelCase
    - example class -> className
    - example for -> htmlFor
    - example onclick -> onClick
    - example onchange -> onChange
[only if you are not using the global css]
    - example style="background-color: red" -> style={{backgroundColor: 'red'}}
*/
import { expect } from "chai";

// we need to write the describe method and add its it

describe("Unit Converter", () => {
    it("should convert km to miles", () => {
        expect(1).to.equal(1);
    });
});
